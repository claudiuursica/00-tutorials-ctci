'use strict'
/**
 * Write a method to replace all spaces in a string with %20
 *
 * Note: In real life, one is better of using encodeURIComponent(), or String.replace()
 */


/**
 * Time:
 * Additional space:
 *
 * @param {string} string - target URL string
 * @returns {string} true if string contains only unique characters
 */
export default function urlIfy(string) {
  return string;
}
